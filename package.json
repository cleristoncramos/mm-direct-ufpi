{
  "name": "dataset-csv",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "sudo fuser -k 6379/tcp ; sudo fuser -k 8081/tcp ; rm -f ./src/logs/sequentialLog.aof ; rm -f ./src/logs/sequentialLog.aof ; mkdir -p ./src/datasets && touch ./src/datasets/datasets.csv && node --max-old-space-size=8192 server.js",
    "cpu": "node cpu-chart.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parser": "^3.0.0",
    "express": "^4.18.2",
    "react-terminal-ui": "^1.2.0",
    "tail": "^2.2.6",
    "ws": "^8.13.0"
  }
}
